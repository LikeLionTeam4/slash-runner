# 빌드 산출물 (전부 npm run build:vendor / dist:mac으로 재생성 가능)
node_modules/
contract-agent/dist/
agent-app/vendor/*.mjs
agent-app/dist/

# legacy-macos는 이 저장소(slash-agent)의 소스가 아니라 별도 참조 자료(vendor 목업)다 —
# 원본 저장소를 수정·재배포하지 않기 위해 이 git 저장소에는 아예 포함하지 않는다.
# (slash-test 루트의 reports/CONTRACT_GAPS.md에서 참조하는 사본이며, 여기서는 로컬에만 둔다.)
legacy-macos/

.DS_Store
*.log
.env
.env.local
